# Vercel CLI link (project/org IDs)
.vercel

# Impeccable local tool state (critique snapshots, cache)
.impeccable

# macOS / editor cruft
.DS_Store
*.swp
node_modules

# Local ticket backlog (not part of the deployed site)
.scratch/
